# Dominion's real dependency list now lives in pyproject.toml (installed
# via `pip install -e ".[dev]"` -- see README.md). This file is kept only
# because some tooling expects a requirements.txt to exist; it intentionally
# points at pyproject.toml instead of duplicating its dependency list, so
# the two can never drift out of sync.
-e .[dev]
