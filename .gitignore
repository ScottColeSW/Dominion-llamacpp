__pycache__/
*.pyc
*.pyo
.DS_Store
Thumbs.db
.claude/settings.local.json
src/dominion/engine/dominion_history.db
src/dominion/agents/player_memos.log
.venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Multi-gigabyte model weights (README.md's "llama.cpp setup" section --
# these are downloaded/placed locally, never committed; way over GitHub's
# 100MB per-file limit anyway).
models/*.gguf

# Raw OBS recordings (OBS's default "YYYY-MM-DD HH-MM-SS.mp4" naming) and
# encoder logs -- source footage stays local. Only the curated, purpose-named
# clips actually referenced from README.md get committed.
Dominion-Video/20*.mp4
Dominion-Video/*.log
